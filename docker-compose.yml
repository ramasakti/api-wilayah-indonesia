version: '3.8'

services:
  api:
    build:
      context: ./app
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - DATA_DIR=/app/data
      - GIN_MODE=release
    volumes:
      - ./data:/app/data
    restart: unless-stopped
